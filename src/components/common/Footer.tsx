import styled from 'styled-components';

const Footer = () => {
  return <FooterWrapper>@Copyright @2022 KimJungJin</FooterWrapper>;
};

const FooterWrapper = styled.footer`
  padding: 20px 0;

  text-align: center;
`;

export default Footer;
